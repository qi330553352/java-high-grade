package com.example.qixin.multithreading;
/**
 * 包说明：
 * 1、此包下文件用来说明Java并发编程
 extends Thread / implements Runnable / implements Callable<T>
 interrupt() / isInterrupted() / interrupted()
 InterrputException / isInterrupted() / interrupt()
 setDaemon(true) / finally
 ThreadLocal / get / set
 volatile
 wait() / notifyAll()
 join()
 notify：唤醒某一个
 notifyAll：唤醒所有

 yield()：不会释放锁的
 sleep()：不会释放锁的
 wait()：调用前持有锁，调用了wait方法以后，锁会被释放
 notity：调用前持有锁，包含了notify() 的方法结束以后，锁才会被释放


 */